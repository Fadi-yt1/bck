{
  "name": "clearcut",
  "version": "1.0.0",
  "private": true,
  "description": "Clearcut — a fast, private background remover powered by the Photoroom API.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "@netlify/functions": "^3.0.0"
  }
}
